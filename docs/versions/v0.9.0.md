# v0.9.0 — Lead Capture & Inquiry Management

> Status: **Stage 1 + Stage 2 Complete — Validation Passed / Sealed; next Stage 3 Operational Validation**  
> Round 1: **Validation Passed / Sealed**  
> Round 2: **Validation Passed / Sealed**  
> Round 3: **Validation Passed / Sealed**  
> Implementation: **Lead metadata in Public Chat + CTA + Inline Inquiry Form + existing Public Inquiry REST**  
> Plugin Version: **0.9.0**  
> DB Version: **1.3**  
> Current stable release remains: **v0.8.0 — Chat Integration**

---

## 1. 版本定位

v0.9.0 不把 WP AI Chat Lab 扩展成完整 CRM，也不进入实时人工客服。

本版本只解决两个业务问题：

1. **什么时候应该自然地邀请访客提交询盘？**
2. **询盘提交后，WordPress 后台如何保存、查看和处理？**

整体路径：

```text
Knowledge
↓
Retrieval
↓
Grounding
↓
Usage Guard
↓
Chat
↓
Lead Trigger Policy
↓
Inquiry Form
↓
Inquiry Service / Repository
↓
wp_wpaic_inquiries
↓
后台 Inquiry Management
```

核心原则：

> **先帮助，再转化。**  
> AI 的首要任务仍然是可靠回答问题；只有在用户主动表达需求、AI 无法继续帮助或业务边界自然出现时，系统才提供询盘入口。

---

## 2. 架构边界

Chat 与 Inquiry 必须保持独立：

```text
Chat Response
     ↓
Lead Trigger Policy
     ↓
Should Offer Lead?
 ┌──────┴──────┐
 No            Yes
 ↓              ↓
继续聊天        CTA
                 ↓
             用户主动点击
                 ↓
           Inline Inquiry Form
                 ↓
           Inquiry Controller
                 ↓
            Inquiry Service
                 ↓
          Inquiry Repository
                 ↓
         wp_wpaic_inquiries
```

Lead Trigger Policy **只决定是否显示邀请**，不能直接保存询盘，也不能强制弹出表单。

---

## 3. Lead Trigger Policy

第一版采用 **Deterministic Policy（确定性规则）**，不额外调用 AI 判断购买意图。

### 3.1 Trigger Types

第一版只保留四类：

```text
manual
commercial_intent
no_answer
usage_blocked
```

### 3.2 manual

用户主动点击：

- Request a Quote
- Contact Sales
- Leave a Message

这是最高确定性的询盘入口。

### 3.3 commercial_intent

当问题包含明确商业需求关键词时，Chat 仍优先正常回答，然后在回答下方显示 CTA。

示例：

```text
Can I get a quote for 500 sqm CWC-610?
↓
正常 answer
+
Request a Quote CTA
```

### 3.4 no_answer

当现有知识不足以可靠回答时：

```text
no_answer
↓
提示当前知识不足
↓
Leave a Message CTA
```

这是“AI 解决不了 → 留给团队后续处理”，不是实时 Human Handoff。

### 3.5 usage_blocked

- `conversation_limit_reached`：主要 CTA 仍是 Start New Conversation，询盘 CTA 只作为次级入口。
- `visitor_daily_limit_reached`：可以明显提供 Leave Your Requirement。
- `site_daily_limit_reached`：AI 暂时不可继续时，询盘是自然 fallback。

### 3.6 不自动触发 Lead 的情况

以下状态默认不显示自动 Lead CTA：

```text
clarify
Weak
Medium
ordinary answer without commercial intent
HTTP 429 request rate limit
HTTP 503 provider error
network error
invalid conversation
```

系统故障、滥用保护或问题不够明确时，不应该借机强制收集个人信息。

---

## 4. Commercial Intent Keywords — 后台完全可配置

这是 v0.9.0 的固定产品原则：

> **商业需求关键词不能写死在代码中。**

插件只提供一组默认值，管理员可以在后台完整控制。

### 4.1 后台能力

第一版设置页至少支持：

- 查看默认关键词；
- 添加关键词；
- 删除关键词；
- 修改关键词；
- 一行一个关键词 / 短语；
- 保存后立即生效；
- 恢复默认关键词；
- 允许保存空列表。

**空列表的语义：关闭 commercial_intent 关键词触发。**

插件不能在管理员清空后自动重新写入默认值；只有管理员主动点击“恢复默认关键词”时才恢复。

### 4.2 默认关键词（初始建议）

```text
quote
quotation
price
pricing
sample
order
MOQ
minimum order
bulk
custom
customized
OEM
ODM
distributor
dealer
shipping
delivery
contact sales
```

默认值只是开箱即用起点，不代表固定规则。

### 4.3 多语言

设置应允许任意 Unicode 文本，因此管理员可以自行增加：

```text
报价
价格
样品
起订量
定制
代理
经销商
交期
联系我们
```

或其它语言关键词。

### 4.4 存储建议

使用 WordPress Option 保存标准化后的关键词数组，例如：

```text
wpaic_lead_commercial_keywords
```

不为关键词单独创建数据库表。

### 4.5 匹配原则

第一版保持简单、可解释：

- Trim + 去除空项；
- 去重；
- Latin 文本大小写不敏感；
- 支持多词短语；
- 不调用 AI；
- 每一次匹配结果都应能从后台配置中解释。

后续若真实数据表明误判明显，再升级规则，不提前引入复杂 NLP / Lead Scoring。

---

## 5. Trigger Priority

当同一请求同时满足多个触发条件时，第一版建议固定优先级：

```text
manual
↓
commercial_intent
↓
no_answer
↓
usage_blocked
```

示例：问题本身明确要求报价，即使最终知识不足，也优先使用 `commercial_intent` 的 Request a Quote CTA，而不是泛化的 Leave a Message。

---

## 6. Inquiry Form

第一版使用 Chat 内联表单，不再打开第二层 Modal。

### 必填

```text
Name
Email
Message
```

### 选填

```text
Company
Phone / WhatsApp
```

暂不增加：

```text
Country
Quantity
Budget
Product selector
Job title
Address
```

真实使用后再根据提交数据决定是否增加。

### 6.1 Message 预填

CTA 打开表单时，可以根据当前问题预填 Message，例如：

```text
I'm interested in CWC-610.
My requirement is approximately 500 sqm.
```

用户可以继续修改。

### 6.2 提交成功

不跳转页面：

```text
Thank you. Your request has been submitted successfully.
Our team will get back to you soon.

[ Continue Chatting ]
```

---

## 7. Inquiry Database

Inquiry 属于运营数据，不属于网站内容，因此第一版使用独立自定义表，而不是 CPT。

计划表名：

```text
wp_wpaic_inquiries
```

### 7.1 建议字段

```text
id
created_at
updated_at
status

name
email
phone
company
message

trigger_type
trigger_reason
conversation_id
source_url
last_question
visitor_hash
```

建议索引：

- `status`
- `created_at`
- `email`（如实际查询需要）

### 7.2 明确不保存

v0.9.0 第一版不自动保存：

```text
完整 Conversation
完整 Chat Transcript
Prompt
AI Answer
原始 Visitor UUID
原始 IP
Provider Token Detail
```

遵循最少采集原则。

### 7.3 DB Version

Stage 1 Round 2 已正式创建 Inquiry Repository / Database：

```text
Plugin Version = 0.9.0
DB Version = 1.2
wp_wpaic_inquiries = created
```

Round 3 只接入正式 Chat UI，不再修改 Schema，因此继续保持 DB Version = 1.2。

---

## 8. Inquiry Status

第一版固定四个状态：

```text
new
contacted
closed
spam
```

后台中文：

```text
新询盘
已联系
已关闭
垃圾
```

暂不加入 CRM 式复杂状态：

```text
Hot / Warm / Cold
Qualified
Negotiating
Won / Lost
Follow Up
```

---

## 9. Public Inquiry API

计划新增独立 REST：

```text
POST /wp-json/wpaic/v1/inquiry
```

Chat 与 Inquiry 不复用同一个 endpoint。

### 前端允许提交

```text
name
email
phone
company
message
conversation_id
trigger_type
```

### 服务器负责

```text
created_at
updated_at
status
visitor_hash
source_url / source context
trigger validation
rate limit
```

不能信任前端自行提交 `status`、`visitor_hash` 或数据库内部字段。

---

## 10. Inquiry Service Boundary

禁止在 REST Controller 中直接散落 `$wpdb->insert()`。

正式结构：

```text
Public Inquiry Controller
        ↓
Inquiry Service
        ↓
Inquiry Repository
        ↓
wp_wpaic_inquiries
```

这样后续后台列表、状态修改、Spam、删除、导出等都复用 Repository。

---

## 11. Spam / Abuse Foundation

第一版只做最小防护：

### Honeypot

隐藏字段，例如：

```text
website
```

正常用户为空；非空直接拒绝。

### Submission Rate Limit

使用短期 Transient，例如：

```text
同一 Visitor：5 submissions / hour
```

不混入 AI Usage Counter。

### Server Validation

至少包括：

```text
Name length
Email format
Message length
Phone length
Company length
Trigger allowlist
Conversation UUID
```

### 暂不加入 Turnstile

真实上线出现明显垃圾询盘后，再评估 Cloudflare Turnstile。

---

## 12. Database Failure Policy

Inquiry 写入与普通 Chat 不同：不能出现“UI 显示提交成功，但数据库实际失败”。

必须：

```text
Repository Insert
↓
明确确认成功
↓
才返回 Public Success
```

数据库写入失败：

```text
HTTP 500
public error
```

严格 fail closed。

---

## 13. Inquiry Admin Management（Stage 2）

后台新增：

```text
WP AI Chat Lab → Inquiries
```

### List

```text
时间
状态
姓名
邮箱
公司
留言摘要
Trigger
来源
```

筛选：

```text
全部
新询盘
已联系
已关闭
垃圾
```

搜索：

```text
Name
Email
Company
Message
```

### Detail

分为三块：

```text
Contact
- Name
- Email
- Phone
- Company

Inquiry
- Message

Context
- Trigger Type
- Trigger Reason
- Last Question
- Source URL
- Conversation ID
- Submitted At
```

右侧仅提供简单 Status 管理。

---

## 13.1 Stage 1 Round 1 — Validation Passed / Sealed

Round 1 只验证 Lead Trigger，不保存询盘：

```text
Commercial Keywords Settings
↓
WPAIC_Lead_Trigger_Policy
↓
Lead Trigger Lab
↓
Offer / No Offer
```

已完成并封板：

- Plugin Version 已进入 `0.9.0`；DB Version 保持 `1.1`；本轮新增数据库表 = 0；
- 新增 `wpaic_lead_commercial_keywords` Option；
- 首次不存在该 Option 时写入默认关键词；
- 管理员可以增删改、一行一个或使用逗号 / 分号分隔；
- 允许保存空列表，并明确表示关闭 `commercial_intent`；
- 空列表不会被插件自动恢复；
- 管理员可主动“恢复默认关键词”；
- 新增正式 `WPAIC_Lead_Trigger_Policy`；
- 新增永久 `Lead Trigger Lab`，直接调用正式 Policy；
- Latin 关键词大小写不敏感，并使用词边界避免 `order` 错误命中 `border`；
- 非 ASCII 关键词使用 Unicode 子串匹配，可支持中文等语言；
- Trigger Priority 固定为 `manual → commercial_intent → no_answer → usage_blocked`；
- `clarify` / `error` 为自动 Lead 硬停止状态，用户主动 `manual` 除外；
- 修复 Settings API 保存回调缺失导致首次保存关键词触发 Critical Error 的问题；保存入口与运行时 Policy 统一复用关键词标准化逻辑。

本轮仍明确不做：

```text
wp_wpaic_inquiries
Public Inquiry REST
Inquiry Repository
Inquiry Form
正式 Chat CTA Integration
Personal Data Capture
AI Lead Scoring
```

### Round 1 真实 WordPress 验收结果

1. 普通 `answer` + `CWC-610 dimension` → No Offer：✅
2. 默认 `quote` + `answer` → `commercial_intent / Request a Quote / primary`：✅
3. 后台新增中文 `报价`，保存后立即命中：✅
4. 删除中文 `报价` 后，同一中文问题立即停止触发：✅
5. 清空全部关键词后 `Keywords = 0 / Commercial Intent = Disabled`，`quote` 不再触发：✅
6. 点击“恢复默认关键词”后 18 个默认词恢复并重新生效：✅
7. 普通未知问题 + `no_answer` → `no_answer / Leave a Message / primary`：✅
8. Visitor Daily Limit → `usage_blocked / Leave Your Requirement / primary`：✅
9. Site Daily Limit → `usage_blocked / Leave Your Requirement / primary`：✅
10. Conversation Limit → `usage_blocked / Contact Us / secondary`：✅
11. `clarify` 即使问题包含 `quote` 也 Hard Stop → No Offer：✅
12. `error` 即使问题包含 `quote` 也 Hard Stop → No Offer：✅
13. `no_answer + quote` 同时成立时，按优先级选择 `commercial_intent`：✅
14. `manual` 在 `error` 状态下仍最高优先 → `manual / Contact Sales / primary`：✅
15. `What is the border dimension?` 不会因默认关键词 `order` 而误触发：✅

**Round 1 结论：Validation Passed / Sealed。** Lead Trigger Lab 与关键词设置永久保留，用于后续规则调整、诊断与回归测试。

Stage 1 Round 2 已完成 Inquiry Database / Repository / Service / Public REST / Validation / Honeypot / Submission Rate，并通过真实 WordPress 验收。Stage 1 Round 3 也已完成正式 Chat CTA、Inline Inquiry Form 与同一 Public Inquiry REST 的真实前台集成验收并封板。Stage 1 现已完成；**Stage 2 Round 1 — Inquiry List + Detail + Status 已进入 Validation Build。**

---

## 14. 版本 Stage 规划

### Stage 1 — Lead Trigger & Inquiry Capture Foundation

只实现：

```text
Lead Trigger Policy
Commercial Keywords Settings
CTA / Inline Inquiry Form
Inquiry Playground / Lab
Public Inquiry REST
Server Validation
Inquiry Service / Repository
wp_wpaic_inquiries
Honeypot
Submission Rate Limit
```

正式 Chat UI 的漂亮 Inline Form 可以在边界验证通过后再接入。

#### Stage 1 核心验收

```text
Manual CTA                      ✅ Round 1 Passed
Commercial Intent               ✅ Round 1 Passed
Keywords 后台增删改             ✅ Round 1 Passed
Keywords 清空 = 关闭触发        ✅ Round 1 Passed
Restore Defaults                ✅ Round 1 Passed
no_answer CTA                   ✅ Round 1 Passed
Visitor / Site blocked CTA      ✅ Round 1 Passed
clarify / error 不自动触发      ✅ Round 1 Passed
429 不自动触发                  ⏳ Stage 3 Operational Validation
503 不自动触发                  ⏳ Stage 3 Operational Validation
Form Validation                 ✅ Round 2 Passed
DB Insert                       ✅ Round 2 Passed
DB Failure forced injection     ⏳ Stage 3 Operational Validation
Honeypot                        ✅ Round 2 Passed
Submission Rate                ✅ Round 2 Passed
```

### Stage 2 — Inquiry Admin Management

#### Round 1 — Inquiry List + Detail + Pagination

Round 1 建立最小后台运营闭环并完成 UI 验证：

```text
wp_wpaic_inquiries
↓
Administrator-only Inquiry List
↓
Inquiry Detail
↓
10 / page Pagination
```

列表使用 WordPress 原生 `wp-list-table / widefat / striped / table-view-list` 视觉结构，详情改为全宽纵向卡片并保持 20px 间距。

#### Round 2 — Read / Unread + Filter + Trash — Validation Passed / Sealed

根据实际运营需求，Inquiry 生命周期进一步简化：

```text
Operational Status: unread / read
Trash Lifecycle: trashed_at NULL / datetime
```

核心规则：

- 新 Inquiry 默认 `unread`。
- 点击“查看详情”自动将未读 Inquiry 标记为 `read`。
- 详情页不再提供手动 Status 下拉框。
- 顶部使用 WordPress 风格筛选：全部 / 未读 / 已读 / 回收站。
- “全部”只统计未进入回收站的 Inquiry。
- 删除采用软删除：先写入 `trashed_at` 并进入回收站。
- 回收站支持恢复与永久删除；永久删除只允许对回收站数据执行。
- Read / Unread 状态与 Trash 生命周期独立，恢复后保留进入回收站前的读状态。
- 分页默认每页 10 条，并保留当前筛选条件。
- Pagination 使用紧凑 WordPress 后台风格：`‹ 第 X / Y 页 ›`。
- Name / Email / Phone / Company / Message / Trigger / Conversation / Source / Visitor Hash 继续保持只读。
- Plugin Version 保持 `0.9.0`。
- DB Version 升至 `1.3`，只新增 `trashed_at` 列并迁移旧状态。

旧状态迁移：

```text
new                → unread
contacted / closed → read
spam               → read + trash
```

Round 2 当前不加入 Search / Bulk Actions / Email Notification / CRM Sync / Human Handoff。

Round 2 Validation Focus：

1. 新 Inquiry 默认未读。
2. 打开详情自动变为已读，返回列表后计数同步。
3. 全部 / 未读 / 已读 / 回收站筛选正确。
4. 分页在不同筛选条件下保持正确计数与页数。
5. 移至回收站后从正常列表消失并进入回收站。
6. 回收站恢复后回到正常列表并保留原读状态。
7. 永久删除只能从回收站执行且删除后不可恢复。
8. 详情页不再存在手动状态设置。

### Stage 3 — Operational Validation

```text
Duplicate Submission
Concurrent Submission
DB Failure
Network Failure
Invalid Input
Permissions
XSS / Escaping
Long Text
Spam
Status Transition
Delete
Privacy Boundary
```

最后进入：

```text
v0.9.0 Final Review
↓
Release
```

---

## 15. v0.9.0 明确不做

```text
Real-time Human Handoff
Agent Inbox / Live Agent
WebSocket / Polling Human Chat
CRM Pipeline
AI Lead Scoring
Automatic Email / CRM Sync
Full Conversation Persistence
RAG
Vector Database
Agent / Tool Calling
```

如果未来需要 Human Handoff，应独立设计，不把 v0.9.0 变成在线客服系统。

---

## 16. Lab / Playground Policy

延续项目既有规则：

> **所有用于教学、诊断、验证架构的 Lab / Playground / Validation 页面长期保留。**

v0.9.0 新增的 Lead Trigger / Inquiry Playground 在版本完成后也不删除，而是作为后续规则调整、数据库诊断和版本回归工具。

---

## 17. 设计结论

v0.9.0 的核心不是“让 Chat 多一个表单”，而是建立一个清晰的业务边界：

```text
先帮助
↓
建立信任
↓
识别明确商业需求或帮助边界
↓
自然邀请询盘
↓
用户主动提交
↓
保存最少必要信息
↓
后台跟进
```

其中 `commercial_intent` 必须始终遵循：

> **默认值只是起点；最终规则由网站管理员在后台完全控制。**



---

## Stage 1 Round 2 — Inquiry Capture Foundation — Validation Passed / Sealed

Round 2 已建立真实 Inquiry Capture Boundary，并完成真实 WordPress 验收；正式 Chat Widget 在本轮仍未修改。

```text
POST /wp-json/wpaic/v1/inquiry
↓
Inquiry Context
↓
Honeypot
↓
Visitor Submission Rate (5 / hour)
↓
Server Validation
↓
Inquiry Service
↓
Inquiry Repository
↓
wp_wpaic_inquiries
```

### 数据库

DB Version 从 `1.1` 升至 `1.2`，新增：

```text
wp_wpaic_inquiries
```

核心字段：`status / name / email / phone / company / message / trigger_type / conversation_id / source_url / last_question / visitor_hash / created_at / updated_at`。

不保存原始 Visitor UUID、不保存 IP、不保存完整 Conversation Transcript、Prompt 或 AI Answer。

### Public Contract

成功：HTTP `201`：

```json
{
  "type": "submitted",
  "message": "Thank you. Your request has been submitted successfully."
}
```

公开响应不返回数据库 ID、Visitor Hash 或其它内部诊断字段。

### Anti-abuse

- Honeypot 在 Rate consumption 之前拒绝。
- 同一 Visitor 默认最多 `5 submissions / hour`。
- Inquiry Rate 与 Chat Public Request Guard / Provider Usage Guard 相互独立。
- 管理员 Lab 只允许重置当前浏览器 Visitor 的 Inquiry Rate，不删除已保存 Inquiry。

### Round 2 Validation Checklist

1. 正常提交 → HTTP 201 / `submitted` / Rows +1。
2. Name / Email / Message 必填校验 → HTTP 400 / 不写库。
3. 非法 Email → HTTP 400 / 不写库。
4. 非法 Conversation UUID → HTTP 400 / 不写库。
5. Trigger 非 allowlist → HTTP 400 / 不写库。
6. Honeypot → HTTP 400 / 不写库 / 不消耗 Inquiry Rate。
7. 同 Visitor 第 6 次提交 → HTTP 429 / `Retry-After` / 不写库。
8. 数据库存储 Visitor Hash，不存 raw Visitor UUID / IP。
9. Repository 写入只有成功后才允许返回 HTTP 201；强制 DB Failure Injection 留到 Stage 3 Operational Validation。

### Round 2 Real Validation Result

真实 WordPress 验收结果：

1. 正常 Public Inquiry → HTTP `201` / `submitted` / Rows +1 / `status = new`：✅
2. Name 必填校验 → HTTP `400` / Rows 不增加：✅
3. 非法 Email → HTTP `400` / Rows 不增加：✅
4. 非法 Conversation UUID → HTTP `400` / Rows 不增加：✅
5. Trigger 非 allowlist → HTTP `400` / Rows 不增加：✅
6. Honeypot → HTTP `400` / Rows 不增加 / Inquiry Rate 不增加：✅
7. 同一 Visitor 重置 Rate 后连续提交：前 5 次成功，第 6 次 HTTP `429` + `Retry-After`，Rows 不增加：✅
8. 数据库只保存 Visitor Hash，不保存 raw Visitor UUID / IP / 完整 Conversation：✅
9. Repository 只有 Insert 成功后才返回 HTTP `201`；强制 DB Failure Injection 保留到 Stage 3：实现边界已就绪，故障注入待后续专项验证。

**Round 2 结论：Validation Passed / Sealed。** Inquiry Capture Lab 永久保留，继续作为 Public REST、Validation、Honeypot、Rate 与 Repository 的教学、诊断和回归工具。

Stage 1 Round 3 已完成：正式 Chat Widget 已复用封板后的 Lead Trigger Policy 与同一 `POST /wp-json/wpaic/v1/inquiry`，没有重新实现 Inquiry 保存逻辑。Round 3 已 Validation Passed / Sealed，Stage 1 现已完成。

### Round 2 明确不做

- 不修改正式 Chat Widget；
- 不增加 Chat Inline Inquiry Form；
- 不进入正式 Inquiry List / Detail / Status 管理；
- 不保存完整聊天记录；
- 不增加 Turnstile；
- 不进入 Human Handoff / CRM。


## 18. Stage 1 Round 3 — Chat Inline Inquiry Form Integration

### 18.0 Fix 1 — Clarify Intent Continuity

真实前台测试发现：商业问题 `Can I get a quote for CWC-610?` 首轮可能得到 `clarify`，访客补充 `CWC-610` 后虽然得到正常 answer，但如果 Lead Policy 只检查当前一句，就会丢失上一轮商业意图。Fix 1 在 sessionStorage 中只保留原始商业问题作为 pending Lead context；下一轮仍由服务器重新运行同一个 `WPAIC_Lead_Trigger_Policy`。该上下文只影响 CTA 选择，不进入 Retrieval / Grounding / Usage / Provider Prompt，也不新增 Conversation / Message 数据表。


> Status: **Validation Passed / Sealed**

Round 3 不重新实现 Lead 或 Inquiry 保存逻辑，只把 Round 1 与 Round 2 两个已封板模块接到正式 Chat Widget：

```text
Public Chat Result
↓
WPAIC_Lead_Trigger_Policy
↓
optional safe lead metadata
↓
Chat CTA
↓
Inline Inquiry Form
↓
POST /wp-json/wpaic/v1/inquiry
↓
Round 2 Service / Repository
```

### 18.1 Public Chat Contract 扩展

只有 Policy 决定 `offer = true` 时，Chat response 才附加最小 `lead` 对象：

```text
trigger_type
cta_label
placement
```

不会公开：

```text
matched_keyword
policy reason
Usage reason code
Provider / Model / Token / Score
```

`clarify`、Provider Error、Chat Request 429 等路径仍不返回自动 Lead。

### 18.2 Manual Entry

正式 Widget 保留轻量的 `Contact Sales` 常驻入口；初始 Quick Question 中的 `Get a Quote` 也直接打开表单。

Manual 打开表单不需要先调用 AI，因此不消耗 Provider Usage。

### 18.3 Inline Form

字段继续沿用已确认的最小范围：

```text
Name *
Email *
Company
Phone / WhatsApp
Message *
```

自动带入：

```text
trigger_type
conversation_id（如果已有）
last_question
source_url
```

Message 默认使用当前 / 最近用户问题；用户可以修改。

### 18.4 Privacy Boundary

Chat 的 sessionStorage 只允许保存：

```text
Conversation ID
Chat message transcript
安全 Lead CTA metadata
```

**不得保存未提交表单中的 Name / Email / Company / Phone / Message。**

刷新页面后 Lead CTA 可以恢复，但正在填写且尚未提交的个人信息必须消失。

### 18.5 Round 3 Real Validation Checklist

0. 商业意图跨 clarify 连续性：`Can I get a quote for CWC-610?` → clarify → `CWC-610` → answer 后仍出现 `Request a Quote`，点击后 Message 预填原始商业问题。

1. Manual `Contact Sales` / `Get a Quote` 直接打开 Inline Form，不产生 Chat / AI 请求。
2. Strong + commercial keyword → Answer 后显示 primary Request a Quote CTA。
3. `no_answer` → Leave a Message CTA。
4. `clarify / error` → 不自动显示 CTA。
5. Conversation Limit → secondary Contact Us；Visitor / Site Daily → primary Lead fallback。
6. Chat blocked 后 Inquiry CTA 仍可提交。
7. Message 默认带入 Last Question，允许用户修改。
8. 正常提交 → HTTP 201 → Chat 内显示成功消息 → Inquiry Row +1。
9. 400 / 429 / 网络失败 → 保留表单并显示错误，不显示假成功。
10. 刷新时安全 CTA / Transcript 可恢复，但未提交的表单个人信息不恢复。
11. `+` New Conversation 清空当前 Conversation 与 Lead CTA，不清 Visitor。
12. 手机端 CTA / Inline Form / 提交 / 关闭可正常使用。

Round 3 已完成真实 WordPress 前台验收并正式封板。本轮只 Commit Stage seal，不 Release；v0.9.0 仍需完成 Stage 2 / Stage 3 / Final Review。


### Round 3 Fix 2 — Explicit Inquiry Draft Discard

Real front-end validation found that after Cancel / ×, reopening the Inline Inquiry Form could be repopulated by browser form-state/autofill behavior even though personal fields were never written to Chat sessionStorage. Fix 2 makes the privacy boundary explicit:

- Cancel / × resets and blanks the live form before removing it.
- Every new form instance uses fresh non-persistent field names and `autocomplete=off` so an unsubmitted draft is not restored on reopen.
- Reopening starts with blank personal fields; Message is rebuilt only from the intended current Lead context/default, never from the discarded draft.
- While the Inquiry Form is open, the normal Chat composer is disabled and the duplicate persistent Contact Sales entry is hidden; both recover after close.
- Client-side errors now distinguish required Name / Email / Message from invalid Email format.

No Inquiry API, Repository, database schema, Lead Trigger Policy, Grounding, Usage, or Provider behavior changed. DB Version remains `1.2`.

### Stage 1 Round 3 Fix 3 — Inquiry-open UI state hardening
- Form 打开时隐藏底部重复 Contact Sales 入口。
- Form 打开时 Chat Composer / Send 明确禁用，并使用专用占位提示。
- 同时使用 JS state、row hidden 与 CSS root-state selector，避免主题 CSS 覆盖浏览器 hidden/disabled 视觉表现。
- 修正 Fix 2 仓库压缩包误包含临时工作目录；后续包只允许一个仓库根目录。

### 18.6 Round 3 Real Validation Result — Passed / Sealed

真实 WordPress 前台已验证：

1. Manual `Contact Sales` 直接打开 Inline Inquiry Form，不产生 Chat / Provider 调用。
2. 真实提交只有在 `/inquiry` 返回 HTTP 201 且 Repository 写入成功后才显示成功消息，后台 Rows +1。
3. `commercial_intent` 正常显示 `Request a Quote`；表单 Message 使用原始商业问题。
4. Fix 1 验证通过：商业问题首轮 `clarify` 时不显示 CTA；补充信息得到 answer 后恢复 `Request a Quote`，且 pending context 不进入 AI Prompt。
5. `no_answer` 显示 `Leave a Message`；提交后数据库保留 `trigger_type = no_answer`。
6. Visitor Daily Limit 显示 primary `Leave Your Requirement`；提交后保留 `trigger_type = usage_blocked`。
7. Conversation Limit 显示 secondary `Contact Us`，新建 Conversation 仍是主恢复路径。
8. 非法 Email 不关闭表单、不制造假成功；修正后可重新提交成功。
9. Chat Inline Form 的 Inquiry Rate 已验证：前 5 次成功，第 6 次 HTTP 429；表单与已填内容保留，数据库 Rows 不增加。
10. Fix 2 验证通过：Cancel / × 丢弃未提交的个人信息草稿，重新打开不恢复 Name / Email / Company / Phone / Message。
11. 刷新隐私边界验证通过：Conversation / transcript 可恢复，但未提交个人信息与 Inquiry Message 草稿不持久化。
12. Fix 3 验证通过：Form 打开时重复 Contact Sales 隐藏；Chat Composer / Send 明确禁用，关闭 Form 后恢复。
13. 手机端 CTA、表单、滚动、输入、提交与关闭均正常。
14. 完整仓库 ZIP 重新检查为单一 `wp-ai-chat-lab/` 根目录，不再包含临时工作副本。

**Round 3 结论：Validation Passed / Sealed。** Chat UI Lab 永久保留 Round 3 的集成、错误恢复、隐私与移动端回归清单。

**Stage 1 结论：Round 1 + Round 2 + Round 3 全部 Validation Passed / Sealed。下一步进入 Stage 2 — Inquiry Admin Management。**


- Stage 2 Round 1 UX2：询盘管理页取消 1180px 最大宽度，使用后台可用宽度；列表切换为 WordPress 原生 `wp-list-table / widefat / striped / table-view-list` 样式与原生 `tablenav` 分页外观；默认每页 10 条，保留详情页 20px 卡片间距。


### Stage 2 Round 2 Real Validation Result

Round 2 completed real WordPress admin validation and is now **Validation Passed / Sealed**.

Validated behavior:

```text
Legacy status migration                         ✅
new → unread                                   ✅
contacted / closed → read                      ✅
spam → read + trash                            ✅
Opening active detail → auto read              ✅
All / Unread / Read / Trash views              ✅
Move to trash                                  ✅
Restore from trash                             ✅
Permanent delete only from trash               ✅
10 items / page                                ✅
Full-width WP admin layout                     ✅
Native wp-list-table / tablenav presentation   ✅
```

Round 2 remains intentionally limited to read state + trash lifecycle. Bulk read/unread, source filter, search and empty-trash are deferred to **Stage 2 Round 3 — Inquiry Management Efficiency**.


#### Round 3 — Inquiry Management Efficiency — Validation Passed / Sealed

Round 3 is the final planned Stage 2 feature round. It keeps the Round 2 data model unchanged and focuses only on day-to-day operator efficiency.

```text
Bulk Read / Unread
+
Source Filter
+
Search
+
Empty Trash
```

Rules:

- Bulk operations only support `mark_read / mark_unread` for selected active inquiries.
- No bulk delete, bulk restore, or bulk permanent delete.
- Source filter allowlist remains the four validated Trigger types.
- Search is limited to Name / Email / Company / Message.
- `All / Unread / Read / Trash`, source filter, search and pagination may be combined.
- Opening an active detail still marks the Inquiry read automatically.
- `Empty Trash` permanently deletes all rows with `trashed_at IS NOT NULL`; active inquiries are never affected.
- Empty Trash requires administrator capability, WordPress nonce, and an explicit browser confirmation.
- DB Version remains `1.3`; no schema changes.

Round 3 Validation Focus:

```text
Bulk select + mark read                         ✅
Bulk select + mark unread                       ✅
No selection guard                              ✅
Source filter                                   ✅
Search: Name / Email / Company / Message        ✅
Filter + Search + Pagination composition        ✅
Row actions preserve filters                    ✅
Empty Trash only deletes trashed rows           ✅
Empty Trash leaves active rows untouched        ✅
```

**Round 3 结论：Validation Passed / Sealed。** 本轮是 Stage 2 最后一轮功能开发；批量已读/未读、来源筛选、搜索、组合筛选/分页与清空回收站均已完成真实 WordPress 后台验收。

**Stage 2 结论：Round 1 + Round 2 + Round 3 已完成并封板。** Inquiry Admin Management 到此停止扩展，不加入 CRM、销售阶段、负责人分配、标签或导出；下一步进入 **Stage 3 — Operational Validation**。
