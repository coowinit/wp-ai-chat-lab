# v0.8.0 — Chat Integration

> 当前状态：Stage 1、Stage 2 均已封板；Stage 2 — Simple Chat UI Integration / Validation Passed / Sealed

## 一、版本目标

v0.8.0 不重新开发 AI Engine，而是把 v0.5.0～v0.7.0 已验证的能力接到真实匿名访客入口：

```text
Visitor
  ↓
Public Chat Boundary
  ↓
Conversation / Visitor / Site Context
  ↓
WPAIC_Grounded_Answer_Service
  ↓
Local Retrieval → Grounding Gate → Usage Guard → AI Provider
```

核心原则：**Chat 只是入口，不拥有 AI 逻辑。**

## 二、参考源码定位

- `simple-live-chat`：Stage 2 前端 UI 基线。
- `wp-live-chat-inquiry`：Visitor / Conversation / REST / State 架构参考，不整体合并。
- `wp-ai-chat-lab`：唯一正式插件主体与 AI 核心。

## 三、Stage 1 已实现

- `POST /wp-json/wpaic/v1/chat`
- Conversation UUID：请求未提供时服务器生成并返回
- Visitor UUID：服务器生成，使用 `wpaic_visitor` HttpOnly Cookie 持久化
- Site Context：服务器生成，Public Request 无法覆盖
- Public Contract：`answer / clarify / no_answer / blocked / error`
- Public Response 不暴露 Retrieval Score、Provider、Model、Token、Usage Hash、内部 Reason Code
- Admin `Chat Boundary` Playground 直接调用真实 Public REST
- Usage Counter 必须为 InnoDB；不满足 transaction / row-lock prerequisite 时 fail closed
- Transaction-critical wpdb failure 明确 rollback + `usage_guard_unavailable`

## 四、数据库

- Plugin Version：`0.8.0`
- DB Version：`1.1`
- 新增表：0
- 仍只有 Knowledge Store + Usage Counter

## 五、Public Request

```json
{
  "question": "What are the dimensions of CWC-610?",
  "conversation_id": "optional UUID v4"
}
```

Public Client 不能控制 Visitor / Site、Provider / Model、Usage Limit、Grounding Threshold、Candidate Limit 或 Top K。

## 六、Stage 1 真实验收结果

真实 WordPress 环境已按 1 → 7 连续完成验收，全部符合预期：


1. 首次匿名 Strong：HTTP 200 / `answer`；服务器创建 Conversation + Visitor；真实 AI Answer 返回。✅
2. 同一 Conversation 第二次 Strong：Conversation / Visitor 均为 existing，Conversation ID 保持不变。✅
3. Weak：HTTP 200 / `clarify`；不进入 Provider。✅
4. Medium：HTTP 200 / `clarify`；不进入 Provider。✅
5. None：HTTP 200 / `no_answer`；不进入 Provider。✅
6. Strong #3：Conversation 已达 2/2，HTTP 200 / `blocked`；Provider 不调用。✅
7. 非法 Conversation ID：HTTP 400 / `error`；在进入 Grounded Answer Service 前拒绝。✅
8. 场景 6 反向确认 Weak / Medium / None 没有消耗 AI Usage。✅
9. Public Response 不出现 Provider、Model、Token、Score、Hash、内部 Reason Code。✅
10. Usage Counter = InnoDB / Transactional = Yes；Public Reservation 前置条件通过。✅

**Stage 1 结论：Validation Passed / Sealed。**

## 七、Stage 1 明确不做

- 正式 Chat Widget
- Conversation / Message 表
- Chat History / Admin Inbox
- Lead / Human Handoff / Agent
- Streaming / Cost Guard / Embedding / Vector / RAG
- Public Endpoint Request Rate Limit（后续 Public Hardening）

## 八、Lab 页面长期保留原则

Chat Boundary Playground 将永久保留。后续正式 Chat UI 上线后，它仍用于直接测试 Public REST、Conversation / Visitor Context、Public Contract 与输入校验。

同时，Knowledge / Retrieval / Grounded AI / Usage Guard 等既有 Lab 页面也继续作为长期教学、诊断和回归工具保留；可以优化展示，但不删除核心测试能力。详见 `docs/lab-validation-policy.md`。

## 九、Stage 2 — Simple Chat UI Integration

Stage 2 将 `simple-live-chat` 的视觉结构迁入插件，但删除原 Demo `replies{}` Answer Engine。前端只负责 UI / Session State，所有问题统一请求 Stage 1 已验证的 Public REST。

已实现：

- 新增 `public/` 前端层：Widget Renderer + 独立 CSS + Vanilla JS + View。
- 前台 Widget 默认关闭，由永久 `Chat UI Lab` 页面显式启用。
- 保留 simple-live-chat 的 Launcher / Panel / Header / Quick Questions / Message Bubble / Mobile Layout。
- Conversation ID 保存到 `sessionStorage`；同一标签页刷新后恢复当前 Conversation 与本地消息气泡。
- Visitor 仍只由服务器 HttpOnly Cookie 管理；前端不读、不写 Visitor ID。
- Quick Questions 与普通输入都调用真实 `/wp-json/wpaic/v1/chat`。
- 请求期间显示 Typing，并禁用输入 / Quick Question / Send，避免同一 UI 重复提交。
- `answer / clarify / no_answer / blocked / error` 统一映射为用户友好的消息气泡。
- `blocked` 后当前 Conversation 停止继续发送；Header 的“新会话”只重置 Conversation + 本标签页消息，不重置 Visitor。
- 前端不显示 Provider / Model / Token / Score / Internal Reason Code。
- 新增永久 `Chat UI Lab` 页面，保存 Enable 开关与 Stage 2 回归路线。
- Stage 2 首轮前台预览修复 Widget 初始化时序：脚本等待 DOM Ready 后再绑定 Launcher / Panel 事件，并用初始化标记避免重复绑定。
- 新增数据库表：0；DB Version 仍为 `1.1`。

### Stage 2 真实前台验收结果

真实 WordPress 前台已完成以下验收，全部符合预期：

1. Launcher / Chat Panel 打开与关闭正常，单纯开关窗口不产生 AI 请求。✅
2. `CWC-610` / `CWC-610 dimension` 可通过真实 Public REST 得到 `answer`。✅
3. `dimension` 正确映射为 `clarify`，不暴露 Weak / Grounding 等内部状态。✅
4. `ZXQ-99999-NOMATCH` 正确映射为 `no_answer`。✅
5. Conversation 达到 2/2 后，下一次 Strong 正确映射为 `blocked`。✅
6. Header “＋”只重置 Conversation / 本标签页消息；Visitor 保持，提升 Visitor / Site 测试额度后新 Conversation 可重新正常回答。✅
7. 同一标签页刷新后 Conversation 与本地 transcript 恢复，Usage 连续；第二次 Strong 正常、第三次 Strong 准确 blocked。✅
8. 快速连续点击发送 / Enter 只产生一个活动请求与一次 Usage Reservation。✅
9. 浏览器 Offline 时显示友好 `error`，请求结束后 UI 解锁；恢复网络后可再次发送并成功回答。✅
10. Slow Network 下 Typing / Pending 状态正常，重复发送仍被阻止，请求完成后 UI 恢复。✅
11. 手机端窄屏布局、输入、消息滚动、Header、新会话与关闭操作均正常。✅

**Stage 2 结论：Validation Passed / Sealed。**

Stage 2 已实际覆盖原计划中一部分 Session & Resilience 场景，因此 Stage 3 不再机械重复慢网、断网、重复发送与手机端测试；后续只聚焦尚未验证的 Public Hardening、Provider Error、Visitor / Site Daily Limit 在正式 Widget 中的边界以及必要的请求级防滥用能力。

## 十、后续

- Stage 3：Public Hardening & Remaining Operational Boundaries（Provider Error、Visitor / Site Daily Limit、Public Endpoint Request Rate Limit / Abuse Boundary 等尚未验证项）。
- Stage 4：真实网页完整 Operational Validation → v0.8.0 Final Review。
